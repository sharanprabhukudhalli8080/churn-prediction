# Customer Churn Prediction

Machine Learning project to predict telecom customer churn using Logistic Regression and deployed with Streamlit.

## Run locally
pip install -r requirements.txt  
streamlit run app.py

📁 FINAL FOLDER STRUCTURE

Your repo should look like:

Customer_Churn_Project/
│── app.py
│── churn_model.pkl
│── scaler.pkl
│── requirements.txt
│── README.md

🚀 AFTER ADDING FILES

Run in terminal:

git add .
git commit -m "Added deployment files"
git push origin main
